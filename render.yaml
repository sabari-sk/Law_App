services:
  - type: web
    name: law-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview -- --host 0.0.0.0
    staticSiteDir: dist
    envVars:
      - key: NODE_ENV
        value: production
